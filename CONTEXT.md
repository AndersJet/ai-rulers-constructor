# Agent 规范脚手架

本项目为目标项目生成并持续维护渐进加载的 Agent 规范。本词汇表记录规则体系的共同用语。

## Language

**规则（ruler）**：
针对明确任务范围的 Agent 行为约束，具有项目事实、批准决定或失败案例依据，并能说明验证方式。
_Avoid_: 临时提示、任意建议

**规则领域（rule domain）**：
围绕一类技术职责组织的规则集合，例如后端、Web 前端、安全或交付。
_Avoid_: 应用模块、Git 仓库

**模块（module）**：
主工程直接纳入的一个 Git submodule，是模块登记的完整单元；内部即使同时包含前端和后端，也不再拆成多个模块。
_Avoid_: 内部服务、规则领域、submodel

**主工程（workspace）**：
将一层直接模块组合为整体开发工作区的项目；模块内部结构不形成主工程的第二层模块登记。
_Avoid_: 某个模块的独立工作区

**模块规则源（module rule source）**：
随模块独立维护、可用于模块独立开发的通用规则权威内容。
_Avoid_: 主工程专属调整、任意可编辑镜像

**模块规则快照（module rule snapshot）**：
主工程从某个模块规则源采纳的、具有明确来源与版本的受管副本。
_Avoid_: 第二个规则源、最新远端规则

**工作区来源快照（working-tree source snapshot）**：
某次同步实际读取的模块规则及相关证据的确定内容集合，可以包含尚未提交的变更。
_Avoid_: Git HEAD 的内容、自动跟随工作区变化的引用

**模块规则导出清单（module rule export manifest）**：
模块明确声明供主工程采纳的规则、必要事实与验证方式的范围。
_Avoid_: 整份子工程规则目录、子工程审阅状态副本

**主工程调整（workspace adjustment）**：
仅适用于某个组合工作区的补充或规则调整，不改变模块规则源本身。
_Avoid_: 自动回写子仓库的规则修改

**有效规则集（effective rules）**：
本次工作区中已获采纳并适用于当前任务的规则，包括该工作区适用的公共约束、模块规则及调整。
_Avoid_: 所有可见规则、所有已下载规则

**项目画像（project profile）**：
目标项目中已观察事实、已批准设计决定、验证方式及未决问题的集合。
_Avoid_: 通用规范、推断即事实

**规则激活（rule activation）**：
规则经过所需审阅后，在明确作用域内被允许用于指导任务的状态；它不等于具体生产操作的授权。
_Avoid_: 永久授权
